import VedioSection from "@/components/vedio-section/VedioSection";
import { HomePageProps } from "@/types/pages.types";

export default function Home(props: HomePageProps) {
  return (
    <div>
      <VedioSection />
    </div>
  );
}
