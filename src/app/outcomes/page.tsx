import { OutcomesPageProps } from '@/types/pages.types';
import React from 'react';

const Outcomes = (props: OutcomesPageProps) => {
  return (
    <div>
      
    </div>
  )
}

export default Outcomes;