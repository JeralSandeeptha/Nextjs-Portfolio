import { HeroComponentProps } from '@/types/components.types';
import React from 'react';

const Hero = (props: HeroComponentProps) => {
  return (
    <div>
      
    </div>
  )
}

export default Hero;
